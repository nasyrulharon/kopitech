# Copy to .env on the VPS. Your Docker Hub username (image is <username>/kopitech:latest)
DOCKERHUB_USERNAME=kopitech

# Docker Hub access token (Read scope is enough). Needed only because the image is
# a PRIVATE repo — Watchtower uses these to pull updates. Keep this file out of git.
DOCKERHUB_TOKEN=dckr_pat_xxxxxxxx
